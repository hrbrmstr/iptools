# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

apt_packages:
  - libgeoip-dev

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
